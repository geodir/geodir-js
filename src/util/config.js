// @flow

  var constants = require('../constants');
  
  const config = {
      API_URL : constants.DEFAULT_ENDPOINT,//'https://api.geodir.com',
      REQUIRE_ACCESS_TOKEN : true,
      ACCESS_TOKEN : ''
  };
  
  module.exports = config;